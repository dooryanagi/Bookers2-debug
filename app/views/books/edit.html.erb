<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>

      <!--エラーの部分テンプレートを呼び出す-->
      <%= render 'layouts/errors', obj: @book %>

  		<%= render 'form', book: @book %>
  		<!--urlの指定が必要、インスタンス変数ではない-->

  		<%= link_to 'Show', @book %> |
  		<%= link_to 'Back', books_path, class: "back" %>
  </div>
</div>