<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>Event Mail</h2>

      <!--イベントはグループに紐づいているため、グループの情報も持ってくる必要がある-->
      <!--→form_withで指定する変数にはグループとイベントの両方の指定が必要-->
      <!--ルーティングやpathに関わるエラーが出る場合は、form_withで送るURLを指定する方法がある-->
      <!--urlに渡すのは@group??@eventかと思った-->
      <!--エラーで見つかりません、と言われたので、直接見つけたい情報が見つかるように指定！→完成！-->
      <%= form_with model: [@group, @event], url: group_events_path(params[:group_id]), local:true do |f| %>

        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :contens %>
          <%= f.text_area :contents, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.submit "送信", class: 'btn btn-success btn-block' %>
        </div>
      <% end %>
    </div>
  </div>
</div>